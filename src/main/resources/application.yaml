spring:
  application:
    name: goldenraspberryawards
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true

app:
  csv-path: movielist.csv